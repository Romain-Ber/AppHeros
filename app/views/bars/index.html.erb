<% content_for :title, "Les Tavernes" %>
<div class="show-list">
  <div class="search-bar-index">
    <%= render "bars/search"%>
  </div>
  <%= render "bars/category"%>
  <div class="d-flex flex-column gap-2">
    <%# <div class="card-info-bar-index"> %>
      <% @bars.each do |bar| %>
        <%= link_to bar_path(bar), class:"bar-card" do %>
          <%= render "card", bar: bar %>
        <% end %>
      <% end %>
    <%# </div> %>
  </div>
</div>

<%# style: "color: inherit;" %>
