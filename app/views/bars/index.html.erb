<% content_for :title do %>
  <% if (params[:query] && !params[:query].empty?) || (params[:q] && !params[:q].empty?) %>
    Les <span><%= @bars.count %></span> Tavernes
  <% else %>
    Les <span><%= Bar.count %></span> Tavernes
  <% end %>
<% end %>

<div class="search-bar-index">
  <%= render "bars/search"%>
</div>
<%= render "bars/category"%>



  <div class="d-flex flex-column gap-2" data-controller="card-animate">
    <%# <div class="card-info-bar-index"> %>
      <% @bars.each do |bar| %>
        <%= link_to bar_path(bar), class:"bar-card", data: {card_animate_target: "card"} do %>
          <%= render "card", bar: bar %>
        <% end %>
    <% end %>
  <%# </div> %>
</div>

<%# style: "color: inherit;" %>
