<% content_for :title do %>
  Les <span><%= Bar.count %></span> Tavernes
<% end %>

<div data-controller="filters">
  <div class="search-bar-index">
    <%= render "bars/search"%>
  </div>
  <%#= render "bars/category"%>
    <div class="bars-filters">
      <div data-filters-target="button" data-action="click->filters#resetFilters" class="bars-filters-btn active">Toutes</div>
      <div data-filters-target="button" data-proximity="<%= current_user.near_bars %>" data-action="click->filters#proximity" class="bars-filters-btn">Les plus proches</div>
      <%#= @bars.near([48.1088321, -1.6937042], 0.5, order: 'distance') %>
      <div data-filters-target="button" data-count="10" data-action="click->filters#maxPlayer" class="bars-filters-btn">Les plus animées</div>
      <div data-filters-target="button" data-count="5" data-action="click->filters#minPlayer" class="bars-filters-btn">Les plus tranquilles</div>
    </div>


  <div class="d-flex flex-column gap-2">
    <%# <div class="card-info-bar-index"> %>
      <% @bars.each do |bar| %>
        <%= link_to bar_path(bar), class:"bar-card", data: {filters_target: "bar", count: bar.player_count, id: bar.id} do %>
          <%= render "card", bar: bar %>
        <% end %>
      <% end %>
    <%# </div> %>
  </div>
</div>

<%# style: "color: inherit;" %>
